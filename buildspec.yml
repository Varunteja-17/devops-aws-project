version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14

  build:
    commands:
      - echo Installing dependencies
      - npm install || echo "No package.json found"

  post_build:
    commands:
      - echo Build completed successfully
